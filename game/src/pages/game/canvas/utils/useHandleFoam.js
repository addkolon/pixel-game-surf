/** @format */

export const useHandleFoam = () => {
  const drawFoam = (context) => {
    context
      .drawImage
      // boat.image,
      // boat.frameX * boat.width,
      // boat.frameY * boat.height,
      // boat.width,
      // boat.height,
      // boat.x,
      // boat.y,
      // boat.width,
      // boat.height
      ();
  };
  return {
    drawFoam,
  };
};
